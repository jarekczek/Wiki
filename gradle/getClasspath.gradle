apply plugin: 'java'

repositories {
  mavenCentral()
}

dependencies {
  compile 'org.glassfish.jersey.core:jersey-client:2.0.1'
}

defaultTasks 'cp'

task cp << {
  configurations.compile.each { print "$it;" }
}